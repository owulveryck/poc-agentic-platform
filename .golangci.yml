version: "2"

linters:
  default: standard
  settings:
    errcheck:
      # Response-encoding errors are deliberately ignored with `_ =` where
      # the connection is already committed; everything else must be checked.
      check-blank: false

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
